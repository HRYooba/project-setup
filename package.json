{
  "name": "project-setup",
  "version": "0.0.0",
  "private": true,
  "description": "project-setup プラグインの開発用ツール（lint / test）。プラグイン本体は .claude-plugin/plugin.json で定義され、この package.json は配布物には含まれない（テンプレートは templates/ 配下のみコピーされる）。",
  "scripts": {
    "lint": "eslint .",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0"
  }
}
